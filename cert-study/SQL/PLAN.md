# SQL 정처기 대비 완성 계획서

**작성일:** 2026-04-10
**목표:** 정처기 실기 SQL 필수 문법 완벽 마무리
**기간:** 3일 (9문제)

---

## 현황

### ✅ 이미 배운 것
- SELECT / FROM
- WHERE
- GROUP BY / HAVING
- 집계함수 (COUNT, SUM, AVG, MAX, MIN)
- JOIN ON (INNER, LEFT, RIGHT)
- COALESCE
- 서브쿼리 (WHERE 기초)
- UNION

### ❌ 남은 것 (정처기 필수)
- ORDER BY (정렬)
- DISTINCT (중복 제거)
- INSERT (데이터 삽입)
- UPDATE (데이터 수정)
- DELETE (데이터 삭제)
- CREATE TABLE (테이블 생성)
- ALTER TABLE (테이블 수정)
- CREATE INDEX (인덱스 생성)
- DROP (삭제)

---

## 학습 계획

### 📅 1일차: 기본 쿼리 (2문제)
**1단계: ORDER BY, DISTINCT**

문제 21: ORDER BY 정렬
- 내림차순 / 오름차순
- 여러 컬럼으로 정렬

문제 22: DISTINCT 중복 제거
- 중복 제거 기본 사용법
- GROUP BY와의 차이

---

### 📅 2일차: 데이터 조작 (3문제)
**2단계: INSERT, UPDATE, DELETE**

문제 23: INSERT 데이터 삽입
- INSERT INTO VALUES 구조
- 컬럼 지정 vs 전체

문제 24: UPDATE 데이터 수정
- UPDATE SET WHERE 구조
- 조건별 수정

문제 25: DELETE 데이터 삭제
- DELETE FROM WHERE 구조
- 조건별 삭제

---

### 📅 3일차: 테이블 조작 (4문제)
**3단계: CREATE, ALTER, DROP, INDEX**

문제 26: CREATE TABLE 테이블 생성
- 테이블 구조 정의
- 데이터 타입 (VARCHAR, INT, DATE 등)

문제 27: ALTER TABLE 테이블 수정
- ADD 컬럼 추가
- MODIFY 컬럼 수정
- DROP COLUMN 컬럼 삭제

문제 28: CREATE INDEX 인덱스 생성
- 인덱스 생성 구조
- 성능 최적화

문제 29: DROP 테이블/뷰 삭제
- DROP TABLE
- DROP VIEW CASCADE

---

## 정처기 범위 확인

### ✅ 포함됨 (정처기 필수)
- SELECT / WHERE / GROUP BY / HAVING
- ORDER BY
- DISTINCT
- INSERT / UPDATE / DELETE
- CREATE TABLE / ALTER TABLE
- CREATE INDEX
- DROP CASCADE
- UNION

### ❌ 제외됨 (범위 밖)
- UNION ALL
- LIMIT
- CASE WHEN
- INTERSECT / EXCEPT
- FULL JOIN
- 윈도우 함수

---

## 학습 진도

| 단원 | 상태 | 진도 |
|------|------|------|
| 1단계 (ORDER BY, DISTINCT) | ⬜ 미시작 | 0/2 |
| 2단계 (INSERT, UPDATE, DELETE) | ⬜ 미시작 | 0/3 |
| 3단계 (CREATE, ALTER, DROP, INDEX) | ⬜ 미시작 | 0/4 |
| **총합** | | **0/9** |

---

## 다음 단계

완료 후:
1. ✅ SQL 기초 완벽 마무리
2. 정처기 SQL 실제 기출문제 풀이 시작
3. 정처기 다른 단원 (Java, C, Database, Network 등) 병행

---

**준비 완료! 1단계 시작! 🚀**
